# AGT-002 — Cierre de rama `fix/agt002-closeout-20260901` (2026-09-01)

**Fecha:** 2026-09-01
**Rama:** `fix/agt002-closeout-20260901`
**Base:** `origin/main` en `00e22eaf7af4cc9712b58e1ef4896728a96f369a`

## 1. Alcance

- Corrección del issue #136: medición autoritativa de `usage` (consumo/tokens) desde el bridge
  (`agt002-hetzner-bridge-client.js`), nunca desde el bloque `usage` auto-declarado por el modelo
  dentro de su propia respuesta.
- Reparación robusta de dos pruebas históricamente frágiles.
- Code splitting del bundle de producción.

## 2. Cambios

- El costo (`cost_usd`) se reporta como `cost_usd: null` cuando el bridge no lo entrega, y como el
  valor explícito sólo cuando el bridge sí lo entrega. La ambigüedad original entre "0" y "no medido"
  quedó resuelta: nunca se infiere `0` por ausencia de dato.
- Ninguno de los cambios toca la ruta de decisión GO/NO-GO ni el gate del Radar.

## 3. TDD y baseline

- Baseline capturado antes de iniciar cambios en esta rama: **1189 tests, 1186 PASS, 2 FAIL
  históricos preexistentes** (`agt002-v3-open-questions-visibility`,
  `tender-opportunity-exit-ui`), **1 SKIP**.
- Los dos fallos históricos fueron corregidos de forma robusta como parte de este cierre (ver §1).

## 4. Gates técnicos — resultado final

```text
Focal usage/contract/runtime/persistence/pipeline/worker
  7/7 PASS

npm test (suite completa)
  1191 tests, 1190 PASS, 0 FAIL, 1 SKIP

npm run check:backend-parity
  PASS

npm run build
  PASS
  Antes:  un solo chunk JS de 829.39 kB (gzip 221.82 kB), con warning de Vite (>500 kB)
  Después: chunk máximo `tenders` 404.55 kB (gzip 105.18 kB), sin warning
  chunkSizeWarningLimit no fue modificado; la mejora es real (code splitting), no ocultamiento del warning

npm audit --audit-level=high
  0 vulnerabilidades

git diff --check
  PASS

Escaneo de secretos
  20 archivos escaneados, 0 hits
```

## 5. Revisión independiente

- Revisión inicial: halló una ambigüedad en el reporte de costo (`0` vs. "no medido").
- Corrección aplicada: `cost_usd: null` cuando el bridge no reporta costo; el valor explícito sólo
  cuando el bridge sí lo reporta (ver §2).
- Re-revisión focal tras la corrección: **APPROVE, sin hallazgos**.

## 6. Seguridad

- `npm audit --audit-level=high`: **0 vulnerabilidades**.
- Escaneo de secretos: **20 archivos, 0 hits**.
- `git diff --check`: **PASS**.

## 7. Producción — verificación predeploy

- SPA responde `200`.
- API protegida responde `401` (comportamiento esperado sin sesión).
- Esta verificación es **predeploy**: confirma que producción estaba sana antes de publicar esta
  rama, no que la rama ya estuviera desplegada en ese momento.

## 8. PRs

- **PR #112:** cerrado como *superseded* (obsoleto/superado por trabajo posterior).
- **PR #10 / PR #12:** pertenecen al proyecto SIIO, no a AGT-002; no fueron tocados.
- **PR #170:** esta rama, mergeada a `main`.

## 9. Integración, publicación y smoke postdeploy (2026-09-01)

- **PR #170 MERGED** 2026-09-01T23:20:12Z; squash `b78981510b61d411450b6e046ccce5c10a9260da` en
  `origin/main`.
- **Issue #136 CLOSED** 2026-09-01T23:20:13Z.
- **GitHub Actions/checks/branch protection: no configurados** en este repositorio. El merge se
  sustentó en los gates locales de §4/§6 más la revisión independiente de §5 (APPROVE), no en CI
  automatizado. No se afirma que "CI pasó".
- **Deploy manual** del árbol exacto igual a `origin/main`: `dpl_Dk3j9CmZVuhV3yr9z8ZFWZawoKR6`,
  estado **Production Ready**, alias `https://seguridad-nacional-crm.vercel.app`.
- **Build en Vercel: PASS.** Chunk máximo productivo `tenders`: 404.79 kB (gzip 105.42 kB).
  **0 vulnerabilidades** reportadas durante `npm install` del build.
- **Smoke postdeploy:**
  - SPA canónica: `200`.
  - Los 9 assets JS/CSS listados en el HTML: todos `200`.
  - API protegida `/api/tender-opportunities`: `401` con mensaje "Debe iniciar sesión.".
  - Pantalla de login visible.
  - Consola del navegador: 0 errores.

## 10. Límites vigentes — qué NO afirma este cierre

- **No se hizo QA de UI autenticada** ni **ejecución real del bridge/modelo** como parte de este
  cierre; ninguna de las dos es un *blocker* de este gate de publicación.
- **No se cambió ningún flag** de configuración/producción como parte de este cierre.
- **El presupuesto real del modelo** sigue sin medirse; se mide únicamente antes del próximo
  despliegue canario, no ahora.
- **El diagnóstico del worker durable post-bridge sigue OPEN y no fue resuelto por el issue #136.**
  El issue #136 (CLOSED) cerró la autoridad de `usage` del PREANÁLISIS RADAR
  (`agt002-radar-preanalysis-usage.js`); es un gap distinto del descrito en
  `docs/architecture/agt002-phase9-runtime-open-gaps.md` §OPEN ("Durable worker post-bridge stage
  diagnostics"), donde el path legado `server/index.js::requestAgt002` sigue sin hooks ni classifier
  y conserva error genérico. No bloquea este cierre; es gate obligatorio antes del próximo canary o
  activación real del worker, y debe cerrarse por TDD preservando retry/claim.
- **No hay GitHub Actions/checks/branch protection configurados** en este repositorio; cualquier
  lectura de este cierre debe apoyarse en los gates locales y la revisión independiente
  documentados aquí, no en un CI inexistente.
- El siguiente paso es humano: Juan elige **una** licitación real —Pereira **o** Procuraduría— para
  el gate de onboarding; no se asume la selección.

Ver también: `docs/operations/2026-09-01-agt002-mentoria-y-proximos-pasos.md` y `CURRENT.md` §16.
