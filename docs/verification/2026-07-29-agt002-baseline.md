# AGT-002 Vig-IA improvement program — baseline verification

**Date:** 2026-07-29

**Branch:** `plan/agt002-vigia-analysis-improvements`

## Commands and results

| Check | Result |
|---|---|
| `node --test tests/*.test.mjs` | exit 1; 246 tests, 245 pass, 1 fail |
| `node --test tests/tender-radar-relevance.test.mjs` | exit 1; isolated baseline failure reproduced |
| `npm run check:backend-parity` | exit 0; `backend parity OK` |
| `npm run build` | exit 0; TypeScript and Vite build completed; existing chunk-size warning only |
| `git diff --check` | exit 0 |

Full suite log: `/tmp/agt002-program-baseline.log` (local transient evidence; no secrets copied here).

## Classified baseline failure

- Test: `tests/tender-radar-relevance.test.mjs`
- Error: `ReferenceError: tenderContextualPhysicalSecurityReason is not defined`
- Classification: pre-existing and outside the AGT-002 analysis-improvement scope.
- Reproduction: the focused test fails before any implementation change on this branch.
- Regression rule: no new failing test is acceptable. This baseline failure must remain the only failure unless it is independently fixed; if fixed upstream, the expected final baseline becomes zero failures.

## Baseline guard

Every phase must run its focused tests, backend parity when applicable, build when frontend/backend contracts change, and `git diff --check`. Before release, the full 246+ test suite must be compared by test identity, not only by exit code.