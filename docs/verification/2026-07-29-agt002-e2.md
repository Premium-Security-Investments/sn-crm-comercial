# Verificación E2 — Vig-IA canónico y sin fallback silencioso

Fecha: 2026-07-29  
Rama: `plan/agt002-vigia-analysis-improvements`

## Alcance verificado

E2 establece que, con `AGT002_CANONICAL_ONLY=true`:

- solo un run `AGT-002` / `agent_ai` / `completed` puede marcarse `canonical=true`;
- los intentos se registran de forma append-only como `queued`, `running`, `completed`, `retry_wait`, `needs_attention` o `unavailable`;
- un error, falta de configuración o falta de capacidad no crea ni presenta un análisis sustituto por reglas;
- el worker conserva el job pendiente con backoff cuando Vig-IA está `unavailable`;
- la reutilización idempotente exige `canonical=true`;
- el selector de análisis actual ignora runs más recientes de reglas y no cae a snapshots históricos;
- la presentación conserva la procedencia tipada (`producer`, `method`, `canonical`) y no permite que `result` la falsifique;
- ambos backends conservan paridad byte a byte.

Con el flag apagado se conserva el comportamiento legacy como rollback funcional y no se consulta la columna `canonical` de la migración 050.

## Persistencia

Migración y rollback:

- `supabase/migrations/050_agt002_canonical_analysis.sql`
- `supabase/rollbacks/050_agt002_canonical_analysis_rollback.sql`

La integración PGlite verificó:

- aplicación sobre datos históricos de reglas;
- rechazo de productor no canónico;
- registro canónico mediante RPC dedicada;
- transiciones append-only;
- inmutabilidad de eventos;
- rollback sin borrar runs históricos ni canónicos.

## Gate focal fresco

Comando: pruebas focales E2 + worker + foundation.

Resultado:

- 12 archivos de prueba;
- 12 pasaron;
- 0 fallaron;
- `npm run check:backend-parity`: `backend parity OK`;
- `npm run build`: código 0;
- `git diff --check`: código 0.

Las tres pruebas UI antiguas que exigían `Generar análisis preliminar` / `Actualizar análisis` se actualizaron para exigir exclusivamente:

- `Analizar con Vig-IA`;
- `Actualizar con Vig-IA`;
- `Volver a analizar con Vig-IA`.

Esas tres pruebas pasaron 3/3.

## Regresión completa

Comando:

```bash
node --test tests/*.test.mjs
```

Resultado:

- pruebas: 255;
- pasaron: 254;
- fallaron: 1;
- único fallo: `tests/tender-radar-relevance.test.mjs`;
- causa: `ReferenceError: tenderContextualPhysicalSecurityReason is not defined`.

El mismo fallo está documentado en la línea base previa y no fue introducido por E2.

Salida conservada en:

- `/tmp/agt002-e2-regression-final.log`

## Routing técnico

Claude Code CLI fue invocado con `--model sonnet` para auditar el bloque. Resolvió canónicamente a `claude-sonnet-5`, pero terminó en `error_max_turns` sin entregar reporte. GPT-5.6 Sol retomó desde la verdad de disco, completó el bloque y ejecutó toda la verificación.

Evidencia:

- `/root/.hermes/metrics/claude-primary-canary/20260729T202924-agt002-e2-audit-sonnet.json`
- `/root/.hermes/metrics/claude-primary-canary/20260729T202924-agt002-e2-audit-execution.json`

## Límites

Esta verificación es local. Aún no se han ejecutado para E2:

- push;
- PR/CI/merge;
- migración 050 remota;
- despliegue;
- activación de `AGT002_CANONICAL_ONLY`;
- canary productivo.
